{
  "name": "@redact-secret/adapter",
  "version": "0.1.0",
  "description": "Shared fail-closed masking primitives for Redact Secret host adapters: mask one leaf, walk a value tree.",
  "license": "MIT",
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "CHANGELOG.md",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "node": "20.x || 22.x || 24.x"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/redact-secret/redact-secret-adapters.git",
    "directory": "packages/adapter"
  },
  "homepage": "https://github.com/redact-secret/redact-secret-adapters/tree/main/packages/adapter#readme",
  "bugs": {
    "url": "https://github.com/redact-secret/redact-secret-adapters/issues"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "peerDependencies": {
    "@redact-secret/core": "^0.1.0-beta.6"
  },
  "devDependencies": {
    "@redact-secret/core": "^0.1.0-beta.6"
  }
}
